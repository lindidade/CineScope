@model IEnumerable<CineScope.Models.Movie>
@{
    ViewData["Title"] = "Filmer";
}

<div class="cs-page-header">
    <h1>Filmer</h1>
    <a asp-action="Create" class="cs-btn-red">
        + Lägg till film
    </a>
</div>

<div class="cs-movie-grid">
    @foreach (var movie in Model)
    {
        <div class="cs-movie-card">

            <!-- Poster -->
            <div class="cs-poster">
                <img src="@movie.PosterUrl" alt="@movie.Title"
                     onerror="this.src='/img/no-poster.jpg'" />
                <div class="cs-rating-badge">
                    ★ @movie.Rating
                </div>
            </div>

            <!-- Info -->
            <div class="cs-card-body">
                <h3 class="cs-card-title">@movie.Title</h3>
                <div class="cs-card-meta">
                    <span class="cs-genre">@movie.Genre</span>
                    <span>@movie.ReleaseYear</span>
                    <span>@movie.Duration min</span>
                </div>
                <p class="cs-card-desc">@movie.Description</p>

                <div class="cs-card-actions">
                    <a asp-action="Details" asp-route-id="@movie.Id" class="cs-btn-red">Detaljer</a>
                    <a asp-action="Edit" asp-route-id="@movie.Id" class="cs-btn-outline">Redigera</a>
                    <a asp-action="Delete" asp-route-id="@movie.Id" class="cs-btn-outline">Ta bort</a>
                </div>
            </div>

        </div>
    }
</div>